# UI
streamlit>=1.35.0

# AI Providers — cài tất cả hoặc chỉ cái bạn dùng
google-generativeai>=0.8.0   # Gemini (free tier)
openai>=1.30.0               # GPT (paid)

# Core
pandas>=2.0.0
openpyxl>=3.1.0
